# Qwen Coder Web

link: https://coder.qwen.ai/setting/caenvironment/    

Script de Configuração para o ambiente qwen coder instalar o deno
```bash
apt-get update && apt-get install -y unzip
curl -fsSL https://deno.land/install.sh | sh -s -- -y
```
